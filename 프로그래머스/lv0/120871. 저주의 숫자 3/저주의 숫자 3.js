function solution(n) {
    var answer = 1;
    for(let i=2; i<n+1; i++) {
        answer++;
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
        if(answer % 3 === 0 || String(answer).includes('3')) {
            answer++;
        }
    }
    return answer;
}